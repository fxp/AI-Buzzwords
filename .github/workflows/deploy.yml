# Deploy is now handled by xiaopingfeng-site repo (Cloudflare Pages).
# This repo is authoring-only: translate.yml and freshness-check.yml still run here
# but content publishing requires syncing to ~/Code/xiaopingfeng-site and pushing there.
#
# GitHub Pages: disabled (migrated 2026-05-09)
# Cloudflare Worker: deployed but has no routes (inactive)
#
# This workflow is intentionally a no-op to avoid failing runs.

name: Deploy (no-op)

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Publishing is handled by xiaopingfeng-site repo. No deployment needed here."
